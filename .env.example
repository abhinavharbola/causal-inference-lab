# LLM diagnostic critique step (src/llm_critique/critique.py)
# Groq is the primary provider, NVIDIA NIM is the fallback. Both free tier.
GROQ_API_KEY=
NVIDIA_NIM_API_KEY=

# Database run logging (src/utils/db.py)
# Optional: if unset, falls back to a local SQLite file at data/local_runs.db
SUPABASE_URL=
SUPABASE_KEY=

# Structured logging (src/utils/logging_config.py)
# Optional: if not set, falls back to plain console logging
LOGFIRE_TOKEN=